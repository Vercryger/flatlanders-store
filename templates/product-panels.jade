section
  ul.nav.nav-pills
    li(ng-class="{ active: tab.isSet(1) }")
      a(href="" ng-click="tab.setTab(1)") Description
    li(ng-class="{ active: tab.isSet(2) }")
      a(href="" ng-click="tab.setTab(2)") Specifications
    li(ng-class="{ active: tab.isSet(3) }")
      a(href="" ng-click="tab.setTab(3)") Reviews

  // Description Tab's Content
  product-description(ng-show="tab.isSet(1)")

  // Spec Tab's Content
  product-specs(ng-show="tab.isSet(2)")

  // Review Tab's Content
  product-reviews(ng-show="tab.isSet(3)") 